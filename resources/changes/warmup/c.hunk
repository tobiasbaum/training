<<<275,org/experiment/editor/browser/VFSDirectoryEntryTableModel.java
	protected void columnMoved(int from, int to) {
		if (from == to)
			return;
		if (from < 1 || from >= getColumnCount())
			return;
		if (to < 1 || to >= getColumnCount())
			return;
		ExtendedAttribute ea = extAttrs.remove(from - 1);
		extAttrs.add(to - 1, ea);
	}
	
<<<275,org/experiment/editor/browser/VFSDirectoryEntryTableModel.java, columnMoved()
