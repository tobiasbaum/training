    /**
     * Liefert den maximalen Wert aus dem Array.
     */
    public static int max(final int[] arr) {
        int m = 0;
        for (final int v : arr) {
            m = Math.max(m, v);
        }
        return m;
    }
