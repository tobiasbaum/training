    /**
     * Liefert einen Bereich aus dem übergebenen String.
	 * Indices zählen ab 1 bzw. -1 (0 ist nicht erlaubt und darf nicht übergeben werden).
	 * Beide Indices sind inklusive.
     * Negative Indices laufen 'rückwärts'. -1 ist somit das letzte Zeichen im String, -2 das vorletzte, etc.
	 * Wenn einer oder beide Indices außerhalb des Strings liegen werden sie auf die Grenzen des Strings
	 * beschränkt. Unter Umständen kommt dadurch der leere String, ein Abbruch erfolgt nicht.
     * @param string Gesamtstring
     * @param startIndex Startindex (!= 0), der normalisiert werden soll.
     * @param endIndex Endindex (!= 0), der normalisiert werden soll.
     * @return Der entsprechende Teilstring oder der leere String, falls der startIndex hinter dem endIndex ist.
     */
    public static String getRange(final String string, final int startIndex, final int endIndex) {
        final int len = string.length();

        int from;
        if (startIndex < 0) {
            from = len + startIndex;
        } else {
            from = startIndex - 1;
        }

        int to;
        if (endIndex < 0) {
            to = len + endIndex;
        } else {
            to = endIndex - 1;
        }

        if (to >= len) {
            to = len - 1;
        }

        if (to < from) {
            return ""; //$NON-NLS-1$
        }

        return string.substring(from, to + 1);
    }
